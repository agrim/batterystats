# iOS App Lane

Reserved for the future universal iPhone/iPad app target, expected to be named `BatteryStatsMobile` in `project.yml`.

Keep iPhone-first app shell code here. Move reusable calculations and value models into `Shared/BatteryCore` instead of duplicating them.
